# Архитектура проекта АЛЕВИТ СТРОЙ

> **Документ описывает архитектуру проекта, структуру файлов, принципы организации кода и процесс сборки.**

## 📐 Общая архитектура

Проект представляет собой **статический лендинг** с модульной архитектурой, построенный на чистом HTML/CSS/JavaScript без использования фреймворков. Используется подход **"исходники → сборка → продакшен"**.

### Архитектурные принципы

1. **Модульность** — код разбит на логические модули и компоненты
2. **Разделение ответственности** — четкое разделение HTML, CSS и JavaScript
3. **Масштабируемость** — структура позволяет легко добавлять новые компоненты
4. **Производительность** — оптимизация для быстрой загрузки
5. **Поддерживаемость** — код легко читать и модифицировать

## 📁 Структура проекта

```
saitgusia/
├── src/                          # Исходные файлы для разработки
│   ├── css/                     # Модульная структура CSS
│   │   ├── base/                # Базовые стили
│   │   │   ├── variables.css    # CSS переменные (цвета, шрифты, отступы)
│   │   │   ├── reset.css        # CSS reset
│   │   │   └── typography.css   # Типографика
│   │   ├── layout/              # Стили макета
│   │   │   ├── container.css    # Контейнеры и секции
│   │   │   └── grid.css         # Сетка
│   │   ├── components/          # Стили компонентов (BEM)
│   │   │   ├── about.css
│   │   │   ├── advantages.css
│   │   │   ├── buttons.css
│   │   │   ├── calculator.css
│   │   │   ├── card.css
│   │   │   ├── contacts.css
│   │   │   ├── cta.css
│   │   │   ├── cta-buttons-after-sections.css
│   │   │   ├── faq.css
│   │   │   ├── floating-buttons.css
│   │   │   ├── footer.css
│   │   │   ├── gallery-modal.css
│   │   │   ├── header.css
│   │   │   ├── hero.css
│   │   │   ├── improved-cta-form.css
│   │   │   ├── map.css
│   │   │   ├── pricing.css
│   │   │   ├── process.css
│   │   │   ├── projects.css
│   │   │   ├── reviews.css
│   │   │   ├── services.css
│   │   │   ├── trust.css
│   │   │   └── works.css
│   │   ├── utilities/           # Утилитарные классы
│   │   │   └── helpers.css
│   │   └── main.css             # Главный файл импортов
│   ├── html/                    # HTML шаблоны и компоненты
│   │   ├── index.template.html  # Главный шаблон страницы
│   │   └── partials/            # HTML компоненты (партиалы)
│   │       ├── head.html        # Мета-теги и подключение стилей
│   │       ├── header.html      # Шапка сайта
│   │       ├── hero.html        # Первый экран
│   │       ├── trust.html       # Блок доверия
│   │       ├── advantages.html  # Преимущества
│   │       ├── pricing.html     # Цены
│   │       ├── projects.html    # Проекты
│   │       ├── works.html       # Наши работы
│   │       ├── process.html     # Этапы строительства
│   │       ├── services.html    # Услуги
│   │       ├── reviews.html     # Отзывы
│   │       ├── about.html       # О компании
│   │       ├── calculator.html  # Калькулятор
│   │       ├── faq.html         # FAQ
│   │       ├── cta.html         # CTA блок
│   │       ├── contacts.html    # Контакты
│   │       ├── footer.html      # Подвал
│   │       ├── floating-buttons.html  # Плавающие кнопки
│   │       └── gallery-modal.html     # Модальное окно галереи
│   └── js/                      # JavaScript модули (ES6+)
│       ├── main.js             # Главный файл инициализации (58 строк)
│       ├── components/          # Компоненты приложения
│       │   ├── data-loader.js   # Загрузка данных из JSON (214 строк)
│       │   ├── ui-components.js # Создание UI элементов (296 строк)
│       │   ├── carousels.js     # Карусели проектов и работ (165 строк)
│       │   ├── forms.js         # Формы и валидация (230 строк)
│       │   ├── gallery.js       # Галерея и модальные окна (220 строк)
│       │   ├── ui.js            # UI компоненты: меню, FAQ, скролл (162 строки)
│       │   ├── contacts.js      # Работа с контактами и Schema.org (82 строки)
│       │   └── notifications.js # Уведомления пользователю (50 строк)
│       └── utils/               # Утилиты
│           ├── fetch.js         # Загрузка JSON файлов (27 строк)
│           ├── format.js        # Форматирование данных (100 строк)
│           └── dom.js           # Утилиты для работы с DOM (40 строк)
│
├── index.html                   # Собранная HTML страница (генерируется)
├── styles.css                   # Скомпилированный CSS (генерируется)
├── js/                          # Скомпилированный JavaScript
│   └── main.js                  # Собранный JS (генерируется Webpack)
│
├── images/                      # Изображения проектов и домов
│   └── projects/                # Фотографии проектов (загружаются через бота)
├── data/                        # JSON данные, управляемые через Telegram-бота
│   ├── projects.json            # Проекты домов
│   ├── contacts.json            # Контактная информация
│   ├── reviews.json             # Отзывы клиентов
│   └── works.json               # Портфолио работ
├── docs/                        # Документация
│   ├── ARCHITECTURE.md          # Этот файл
│   ├── DEVELOPMENT_RULES.md
│   └── HTML_COMPONENTIZATION_OPTIONS.md
│
├── scripts/                     # Вспомогательные скрипты
│   ├── build-html.js            # Сборка HTML из шаблона и партиалов
│   ├── split-css.js             # Скрипт для разбиения CSS
│   └── split-html.js            # Скрипт для разбиения HTML
│
├── telegram-bot/                # Telegram-бот для управления контентом
│   ├── bot.js                   # Главный файл бота
│   ├── config.js                # Конфигурация бота
│   ├── handlers/                # Обработчики команд
│   │   ├── contacts.js          # Управление контактами
│   │   ├── projects.js          # Управление проектами
│   │   ├── reviews.js           # Управление отзывами
│   │   └── works.js             # Управление работами
│   └── utils/                   # Утилиты бота
│       ├── auth.js              # Авторизация
│       ├── data-manager.js      # Управление JSON данными
│       └── file-manager.js      # Управление файлами изображений
├── postcss.config.js            # Конфигурация PostCSS
├── webpack.config.js            # Конфигурация Webpack
├── package.json                 # Зависимости и скрипты
└── .cursorrules                 # Правила разработки
```

## 🎨 Архитектура CSS

### Модульная структура (ITCSS методология)

CSS организован по принципу **ITCSS (Inverted Triangle CSS)** с элементами **BEM** для именования компонентов:

```
src/css/
├── base/           # Фундамент (переменные, reset, типографика)
├── layout/         # Структура страницы (контейнеры, сетка)
├── components/     # UI компоненты (кнопки, формы, карточки)
└── utilities/      # Утилиты (помощники, переопределения)
```

### Процесс сборки CSS

1. **Исходники** (`src/css/main.css`) — содержит только `@import` директивы
2. **PostCSS обработка** — объединяет все модули в один файл
3. **Результат** (`styles.css`) — готовый CSS для продакшена

**Команды:**
```bash
npm run build:css      # Одноразовая сборка
npm run watch:css      # Автоматическая пересборка при изменениях
```

### PostCSS плагины

- **postcss-import** — разрешает `@import` и объединяет файлы
- **postcss-preset-env** — современные CSS фичи (nesting, custom properties)
- **autoprefixer** — автоматические вендорные префиксы

### BEM именование

```css
/* Блок */
.hero { }

/* Элемент */
.hero__title { }
.hero__content { }

/* Модификатор */
.btn--primary { }
.btn--gold { }
.card--featured { }
```

## 💻 Архитектура JavaScript

### Модульная структура (ES6+)

JavaScript организован по принципу **модульной архитектуры** с использованием ES6 модулей (`import`/`export`). Код разбит на логические модули по функциональности.

### Структура JavaScript модулей

```
src/js/
├── main.js                      # Главный файл инициализации (58 строк)
│                                # - Импортирует все модули
│                                # - Инициализирует приложение при DOMContentLoaded
│
├── components/                  # Компоненты приложения
│   ├── data-loader.js          # Загрузка данных из JSON (214 строк)
│   │                           # - loadProjects() - загрузка проектов
│   │                           # - loadContacts() - загрузка контактов
│   │                           # - loadReviews() - загрузка отзывов
│   │                           # - loadWorks() - загрузка работ
│   │
│   ├── ui-components.js        # Создание UI элементов (296 строк)
│   │                           # - createContactItem() - элемент контакта
│   │                           # - createSocialLinks() - ссылки мессенджеров
│   │                           # - createProjectCard() - карточка проекта
│   │                           # - createVideoReviewCard() - карточка видео отзыва
│   │                           # - createTextReviewItem() - текстовый отзыв
│   │                           # - createWorkCard() - карточка работы
│   │                           # - escapeHtml() - экранирование HTML (XSS защита)
│   │
│   ├── carousels.js            # Карусели проектов и работ (165 строк)
│   │                           # - initProjectsCarousel() - карусель проектов
│   │                           # - initWorksCarousel() - карусель работ
│   │                           # - initProjectsGalleryButtons() - кнопки галереи проектов
│   │                           # - initWorksGalleryButtons() - кнопки галереи работ
│   │
│   ├── forms.js                # Формы и валидация (230 строк)
│   │                           # - validatePhoneInput() - валидация телефона
│   │                           # - initCTAForm() - CTA форма с прогресс-баром
│   │                           # - initContactForm() - форма контактов
│   │                           # - initCalculator() - калькулятор стоимости
│   │
│   ├── gallery.js              # Галерея и модальные окна (220 строк)
│   │                           # - initGalleryModal() - инициализация модального окна
│   │                           # - openGallery() - открытие галереи
│   │                           # - loadGalleryImages() - загрузка изображений
│   │                           # - updateGalleryThumbnails() - обновление миниатюр
│   │                           # - navigateGallery() - навигация по галерее
│   │                           # - handleGalleryKeyboard() - обработка клавиатуры
│   │
│   ├── ui.js                   # UI компоненты (162 строки)
│   │                           # - initMobileMenu() - мобильное меню-бургер
│   │                           # - initFAQ() - FAQ аккордеон
│   │                           # - initSmoothScroll() - плавная прокрутка
│   │                           # - initFloatingButtons() - плавающие кнопки
│   │                           # - initScrollAnimation() - анимация при скролле
│   │
│   ├── contacts.js             # Работа с контактами (82 строки)
│   │                           # - updateFloatingButtons() - обновление плавающих кнопок
│   │                           # - updateSchemaOrg() - обновление Schema.org данных
│   │
│   └── notifications.js        # Уведомления (50 строк)
│                               # - showNotification() - показ уведомлений
│
└── utils/                      # Утилиты
    ├── fetch.js                # Загрузка данных (27 строк)
    │                           # - fetchJSON() - универсальная загрузка JSON
    │
    ├── format.js               # Форматирование данных (100 строк)
    │                           # - formatPrice() - форматирование цены
    │                           # - formatPhone() - форматирование телефона
    │                           # - parseAddress() - парсинг адреса для Schema.org
    │                           # - getWorkStatusBadge() - текст бейджа статуса работы
    │
    └── dom.js                  # Утилиты DOM (40 строк)
                                # - $ - объект с методами select, selectAll, create, ready
```

### Принципы организации

1. **Модульность** — каждый модуль отвечает за свою функциональность
2. **Размер файлов** — все модули < 800 строк (соответствие `.cursorrules`)
3. **Размер функций** — большинство функций < 30 строк, длинные разбиты на части
4. **JSDoc документация** — все функции имеют полные JSDoc комментарии с `@param` и `@returns`
5. **ES6+ синтаксис** — используется современный JavaScript (const/let, arrow functions, async/await)
6. **Безопасность** — XSS защита через `escapeHtml()` для всех пользовательских данных
7. **Обработка ошибок** — try/catch для асинхронных операций

### Импорты и зависимости

Все модули используют ES6 `import`/`export`:

```javascript
// Пример импортов в main.js
import { loadProjects, loadContacts, loadReviews, loadWorks } from './components/data-loader.js';
import { showNotification } from './components/notifications.js';
import { initCTAForm, initCalculator, initContactForm } from './components/forms.js';
import { initSmoothScroll, initFloatingButtons, initScrollAnimation, initMobileMenu, initFAQ } from './components/ui.js';
import { initGalleryModal } from './components/gallery.js';
```

### Инициализация приложения

`main.js` содержит только инициализацию:

```javascript
document.addEventListener('DOMContentLoaded', function() {
    // Загрузка данных из JSON файлов
    loadProjects();
    loadContacts();
    loadReviews();
    loadWorks();
    
    // Инициализация UI компонентов
    initSmoothScroll();
    initFloatingButtons();
    initScrollAnimation();
    initMobileMenu();
    initFAQ();
    initGalleryModal();
    
    // Инициализация форм
    initContactForm();
    initCTAForm();
    initCalculator();
});
```

### Динамическая загрузка данных

Приложение загружает данные из JSON файлов при инициализации:

- `data/projects.json` — проекты домов
- `data/contacts.json` — контактная информация
- `data/reviews.json` — отзывы клиентов
- `data/works.json` — портфолио работ

Все данные управляются через Telegram-бота (`telegram-bot/`), что позволяет изменять контент без изменения кода.

### Структура данных

**`data/projects.json`:**
```json
{
  "projects": [
    {
      "id": "1",
      "title": "Проект дома",
      "area": 120,
      "price": 3500000,
      "floors": "1 этаж",
      "mainImage": "images/projects/...",
      "gallery": ["images/projects/..."],
      "status": "published"
    }
  ]
}
```

**`data/contacts.json`:**
```json
{
  "phone": "+7 (XXX) XXX-XX-XX",
  "email": "email@example.com",
  "address": "Адрес компании",
  "whatsapp": "https://wa.me/...",
  "telegram": "https://t.me/..."
}
```

**`data/reviews.json`:**
```json
{
  "reviews": [
    {
      "clientName": "Имя клиента",
      "houseName": "Название дома",
      "reviewText": "Текст отзыва",
      "videoUrl": "https://youtube.com/...",
      "isVideo": true,
      "status": "published"
    }
  ]
}
```

**`data/works.json`:**
```json
{
  "works": [
    {
      "title": "Название работы",
      "area": 120,
      "format": "Под ключ",
      "workStatus": "Построен",
      "description": "Описание",
      "mainImage": "images/projects/...",
      "gallery": ["images/projects/..."],
      "status": "published"
    }
  ]
}
```

### Процесс сборки JavaScript

1. **Исходники** (`src/js/`) — ES6+ модули с `import`/`export`
   - `main.js` импортирует все необходимые модули
   - Модули в `components/` и `utils/` экспортируют функции
   
2. **Webpack** — объединяет все модули в один файл:
   - Разрешает все `import`/`export` директивы
   - Транспилирует ES6+ в ES5 через Babel
   - Минифицирует код в production режиме
   
3. **Результат** (`js/main.js`) — готовый JS для продакшена:
   - Один файл со всеми модулями
   - Совместимый с браузерами ES5+
   - Минифицированный и оптимизированный

**Команды:**
```bash
npm run build:js      # Одноразовая сборка (production)
npm run watch:js      # Автоматическая пересборка при изменениях (development)
```

**Зависимости сборки:**

```
main.js
  ├── components/data-loader.js
  │   ├── utils/fetch.js
  │   ├── utils/format.js
  │   ├── components/ui-components.js
  │   ├── components/contacts.js
  │   └── components/carousels.js
  ├── components/notifications.js
  ├── components/forms.js
  │   ├── components/notifications.js
  │   └── utils/format.js
  ├── components/ui.js
  └── components/gallery.js
```

## 🏗️ Архитектура HTML

### Компонентная структура

HTML организован по принципу **компонентной архитектуры** с использованием шаблонов и партиалов:

- **Шаблон** (`src/html/index.template.html`) — основной каркас страницы с директивами `@include`
- **Партиалы** (`src/html/partials/`) — отдельные компоненты секций
- **Сборка** (`scripts/build-html.js`) — скрипт объединяет шаблон и партиалы в `index.html`

### Структура страницы

`index.html` собирается из шаблона и содержит следующие секции (в порядке отображения):

1. **Header** (`header.html`) — навигация и логотип
2. **Hero** (`hero.html`) — первый экран с CTA
3. **Trust** (`trust.html`) — блок доверия (статистика)
4. **Advantages** (`advantages.html`) — преимущества компании
5. **Pricing** (`pricing.html`) — форматы домов и цены
6. **Projects** (`projects.html`) — типовые проекты
7. **Works** (`works.html`) — портфолио построенных домов
8. **Process** (`process.html`) — этапы строительства
9. **Services** (`services.html`) — дополнительные услуги
10. **Reviews** (`reviews.html`) — отзывы клиентов
11. **About** (`about.html`) — о компании
12. **Calculator** (`calculator.html`) — калькулятор стоимости
13. **FAQ** (`faq.html`) — часто задаваемые вопросы
14. **CTA** (`cta.html`) — призыв к действию
15. **Contacts** (`contacts.html`) — контакты и формы
16. **Footer** (`footer.html`) — подвал сайта
17. **Floating Buttons** (`floating-buttons.html`) — плавающие кнопки (WhatsApp, Telegram)
18. **Gallery Modal** (`gallery-modal.html`) — модальное окно для галереи

### Процесс сборки HTML

1. **Исходники** (`src/html/index.template.html`) — шаблон с директивами `<!-- @include partials/... -->`
2. **Скрипт сборки** (`scripts/build-html.js`) — заменяет директивы на содержимое партиалов
3. **Результат** (`index.html`) — готовый HTML для продакшена

**Команды:**
```bash
npm run build:html     # Одноразовая сборка
npm run watch:html     # Автоматическая пересборка при изменениях
```

### Семантическая разметка

- Используются семантические HTML5 теги (`<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`)
- Правильная иерархия заголовков (один `<h1>` на страницу)
- Accessibility атрибуты (`aria-label`, `role`)

## 🔄 Процесс разработки

### Workflow

```
Разработка → Сборка → Тестирование → Продакшен
    ↓           ↓           ↓            ↓
  src/      build/      tests/      deploy/
  
src/js/components/     →    js/main.js
src/js/utils/          →    (объединяется в main.js)
src/css/               →    styles.css
src/html/              →    index.html
data/*.json            →    (загружаются динамически)
```

### Модульная разработка JavaScript

При работе с JavaScript модулями:

1. **Создание нового модуля:**
   - Выбрать категорию: `components/` (компоненты) или `utils/` (утилиты)
   - Создать файл `new-module.js` в соответствующей папке
   - Добавить JSDoc комментарии для всех функций
   - Экспортировать функции через `export`

2. **Использование модуля:**
   - Импортировать в `main.js` или в другом модуле
   - Использовать в `DOMContentLoaded` или в соответствующем обработчике

3. **Проверка размера:**
   - Файл должен быть < 800 строк
   - Функции должны быть < 30 строк (при необходимости разбить)

4. **Сборка:**
   - Webpack автоматически объединит все модули
   - В development режиме доступны source maps
   - В production режиме код минифицируется

### Команды разработки

```bash
# Локальная разработка
npm start              # Запуск live-server на порту 3000 (игнорирует src/)

# Разработка с автоперезагрузкой
npm run dev            # Live-server с watch режимом

# Сборка для продакшена
npm run build          # Собирает HTML, CSS и JS
npm run build:html     # Только HTML
npm run build:css      # Только CSS
npm run build:js       # Только JavaScript

# Наблюдение за изменениями
npm run watch          # Автоматическая пересборка HTML, CSS и JS
npm run watch:html     # Только HTML
npm run watch:css      # Только CSS
npm run watch:js       # Только JavaScript

# Вспомогательные команды
npm run split:html     # Разбить HTML на компоненты

# Качество кода
npm run lint           # Проверка ESLint
npm run format         # Форматирование Prettier
npm test               # Запуск тестов Jest
```

### Сборка проекта

**HTML сборка:**
```bash
node scripts/build-html.js
```
Скрипт читает `src/html/index.template.html`, заменяет директивы `<!-- @include partials/... -->` на содержимое соответствующих файлов из `src/html/partials/` и сохраняет результат в `index.html`.

**CSS сборка:**
```bash
postcss src/css/main.css -o styles.css
```
PostCSS обрабатывает `src/css/main.css`, разрешает все `@import` директивы, применяет плагины (nesting, autoprefixer) и сохраняет результат в `styles.css`.

**JavaScript сборка:**
```bash
webpack --mode=production
```
Webpack:
1. Начинает с точки входа `src/js/main.js`
2. Рекурсивно разрешает все `import` директивы
3. Объединяет все модули в один файл
4. Транспилирует ES6+ в ES5 через Babel (`@babel/preset-env`)
5. Минифицирует код в production режиме
6. Сохраняет результат в `js/main.js`

**Особенности сборки:**
- Все ES6 модули объединяются в один файл
- Поддерживается tree-shaking (удаление неиспользуемого кода)
- Source maps генерируются в development режиме
- Минификация включена в production режиме

## 📦 Зависимости и инструменты

### Основные инструменты

- **PostCSS** — обработка CSS (импорты, автопрефиксер, современные фичи)
- **Webpack** — сборка JavaScript модулей
- **Babel** — транспиляция ES6+ в ES5
- **Live-server** — локальный сервер для разработки

### Dev зависимости

- `postcss`, `postcss-cli`, `postcss-import`, `postcss-preset-env`
- `autoprefixer`
- `webpack`, `webpack-cli`, `babel-loader`, `@babel/core`, `@babel/preset-env`
- `eslint`, `prettier`, `jest`

## 🎯 Принципы организации кода

### Размер файлов

- **CSS модуль** — до 5 KB (до 200 строк), норма < 500 строк, критично > 800 строк
- **JavaScript модуль** — норма < 500 строк (< 20 KB), критично > 800 строк (> 30 KB)
- **HTML секция** — логически завершенный блок
- **JavaScript функция** — рекомендуется ~30 строк, максимум 50 строк

**Текущее соответствие:**
- `main.js`: 58 строк ✅
- Все компоненты: < 300 строк ✅
- Все утилиты: < 100 строк ✅

### Именование

- **CSS классы:** BEM (`.block__element--modifier`)
- **JavaScript:** camelCase для переменных/функций, PascalCase для классов
- **Файлы:** kebab-case (`form-validator.js`, `price-calculator.js`)

### Импорты и зависимости

- **CSS:** все импорты в `src/css/main.css`
- **JavaScript:** ES6 модули (`import`/`export`), все модули используют строгий режим
- **Порядок импортов:** утилиты → компоненты → главный файл
- **Циклические зависимости:** отсутствуют, структура однонаправленная

## 🔍 Точки расширения

### Добавление нового компонента

1. **CSS:**
   - Создать `src/css/components/new-component.css`
   - Добавить `@import './components/new-component.css';` в `src/css/main.css` (в правильном порядке)

2. **HTML:**
   - Создать `src/html/partials/new-component.html` с разметкой компонента
   - Добавить `<!-- @include partials/new-component.html -->` в нужное место в `src/html/index.template.html`
   - Запустить `npm run build:html` для сборки

3. **JavaScript:**
   - Создать новый модуль в `src/js/components/new-component.js` или `src/js/utils/new-utility.js`
   - Добавить JSDoc комментарии с `@param` и `@returns`
   - Экспортировать функции через `export`
   - Импортировать в `src/js/main.js` и вызвать в `DOMContentLoaded`
   - Убедиться, что размер файла < 800 строк и функции < 30 строк

### Интеграция с Backend

Текущая архитектура готова к интеграции:

- Формы отправляют данные через `fetch` API (временно используется имитация)
- Можно легко добавить модуль `src/js/utils/api.js` для централизованной работы с API
- Структура позволяет добавить состояние приложения (например, через простой state manager)
- Данные загружаются динамически из JSON файлов, управляемых через Telegram-бота

### Управление контентом через Telegram-бота

Контент сайта управляется через Telegram-бота (`telegram-bot/`):

**Функциональность бота:**

- **Проекты** (`handlers/projects.js`):
  - Добавление новых проектов домов
  - Редактирование существующих проектов
  - Публикация/скрытие проектов
  - Загрузка фотографий проектов
  
- **Контакты** (`handlers/contacts.js`):
  - Обновление телефона, email, адреса
  - Обновление ссылок на мессенджеры (WhatsApp, Telegram)
  - Автоматическое обновление Schema.org данных
  
- **Отзывы** (`handlers/reviews.js`):
  - Добавление видео отзывов (YouTube ссылки)
  - Добавление текстовых отзывов
  - Публикация/скрытие отзывов
  
- **Работы** (`handlers/works.js`):
  - Добавление завершенных работ с фотографиями
  - Редактирование описаний и характеристик
  - Публикация/скрытие работ

**Архитектура бота:**

- Авторизация через пароль (`utils/auth.js`)
- Управление JSON данными (`utils/data-manager.js`)
- Загрузка и управление файлами изображений (`utils/file-manager.js`)
- Все изменения сохраняются в соответствующие JSON файлы в `data/`
- Фронтенд автоматически загружает обновленные данные при следующей загрузке страницы

**Команды запуска:**

```bash
npm run bot          # Запуск бота
npm run bot:dev      # Запуск бота с автоперезагрузкой (nodemon)
```

## 📊 Производительность

### Оптимизации

- **CSS:** объединение всех модулей в один файл (меньше HTTP запросов)
- **JavaScript:** минификация через Webpack в production режиме
- **Изображения:** lazy loading, WebP формат
- **Кэширование:** статические ресурсы кэшируются браузером

### Метрики

- **First Contentful Paint (FCP):** < 1.8 сек
- **Largest Contentful Paint (LCP):** < 2.5 сек
- **Time to Interactive (TTI):** < 3.8 сек

## 🚀 Деплой

### Продакшен сборка

```bash
npm run build
```

Результат:
- `styles.css` — готовый CSS
- `js/main.js` — готовый JavaScript
- `index.html` — HTML страница

### Развертывание

Проект готов к деплою на любой статический хостинг:
- GitHub Pages
- Netlify
- Vercel
- Обычный веб-сервер (nginx, Apache)

## 📝 Примечания

### Важные правила

- **Не редактировать напрямую:** `index.html`, `styles.css` и `js/main.js` генерируются автоматически
- **Исходники:** Все изменения делаются в `src/`, затем собираются через `npm run build`
- **HTML:** Редактируется в `src/html/index.template.html` и `src/html/partials/`, затем собирается через `npm run build:html`
- **JavaScript:** Модули в `src/js/components/` и `src/js/utils/`, сборка через Webpack
- **Данные:** JSON файлы в `data/` управляются через Telegram-бота, не редактировать вручную

### Рекомендации

- **Разработка:** Использовать `npm run watch` для автоматической пересборки всех файлов
- **Сервер:** Использовать `npm start` для локального сервера (игнорирует папку `src/` и работает с собранными файлами)
- **Модули:** Создавать новые модули в соответствующих папках (`components/` или `utils/`)
- **Размер:** Следить за размером файлов и функций (соответствие `.cursorrules`)
- **JSDoc:** Всегда добавлять полные JSDoc комментарии с `@param` и `@returns`
- **Безопасность:** Использовать `escapeHtml()` для всех пользовательских данных перед вставкой в `innerHTML`

### Процесс работы с данными

1. **Управление контентом:** Использовать Telegram-бота (`npm run bot`)
2. **Изменения данных:** Все изменения сохраняются в JSON файлы в `data/`
3. **Загрузка на фронтенде:** Данные автоматически загружаются при каждой загрузке страницы
4. **Обновление:** После изменений через бота обновите страницу в браузере

### Процесс работы с кодом

1. **Редактирование:** Вносите изменения в файлы в `src/`
2. **Сборка:** Запустите `npm run build` или используйте `npm run watch`
3. **Проверка:** Откройте `index.html` в браузере или используйте `npm start`
4. **Тестирование:** Проверьте работу всех функций в браузере

## 🔧 Конфигурация сборки

### PostCSS конфигурация (`postcss.config.js`)

- **postcss-import** — разрешает `@import` и объединяет файлы
- **postcss-preset-env** — современные CSS фичи (nesting, custom properties)
- **autoprefixer** — автоматические вендорные префиксы

### Webpack конфигурация (`webpack.config.js`)

```javascript
{
  entry: './src/js/main.js',      // Точка входа
  output: {
    filename: 'main.js',           // Имя выходного файла
    path: path.resolve(__dirname, 'js'),  // Папка для выходного файла
    clean: true                    // Очистка папки перед сборкой
  },
  module: {
    rules: [
      {
        test: /\.js$/,             // Обработка .js файлов
        exclude: /node_modules/,   // Исключить node_modules
        use: {
          loader: 'babel-loader',  // Использовать Babel loader
          options: {
            presets: ['@babel/preset-env']  // Транспиляция ES6+ в ES5
          }
        }
      }
    ]
  },
  resolve: {
    extensions: ['.js']            // Разрешать импорты без расширения
  },
  mode: process.env.NODE_ENV || 'development'  // Режим сборки
}
```

**Особенности конфигурации:**
- **Entry:** `src/js/main.js` — главный файл с импортами всех модулей
- **Output:** `js/main.js` — один собранный файл для продакшена
- **Babel:** транспиляция ES6+ в ES5 через `@babel/preset-env`
- **Mode:** development (watch) или production (build)
- **Clean:** автоматическая очистка выходной папки перед сборкой
- **Resolve:** поддержка импортов без расширения `.js`

## 🔒 Безопасность

### XSS защита

Все пользовательские данные экранируются перед вставкой в DOM:

- Используется функция `escapeHtml()` для экранирования HTML
- Данные из JSON файлов экранируются перед использованием в `innerHTML`
- Валидация форм на клиентской стороне (телефон, email)

### Валидация данных

- Валидация телефонных номеров в формате +7 (XXX) XXX-XX-XX
- Проверка обязательных полей форм
- Визуальная индикация ошибок валидации

## 📋 Соответствие правилам разработки

Проект полностью соответствует правилам из `.cursorrules`:

- ✅ **Размер файлов:** все JS файлы < 800 строк
- ✅ **Размер функций:** большинство функций < 30 строк, длинные разбиты
- ✅ **JSDoc:** все функции имеют полную документацию
- ✅ **Модульность:** ES6 модули, четкое разделение ответственности
- ✅ **ES6+ синтаксис:** const/let, arrow functions, async/await
- ✅ **Обработка ошибок:** try/catch для асинхронных операций
- ✅ **Безопасность:** XSS защита через escapeHtml()
- ✅ **Именование:** camelCase для функций, UPPER_SNAKE_CASE для констант
- ✅ **Форматирование:** Intl.NumberFormat для чисел, форматирование телефонов

---

**Последнее обновление:** 2024  
**Версия архитектуры:** 3.0 (модульная структура JavaScript)
