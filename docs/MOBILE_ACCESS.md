# 📱 Доступ к сайту с мобильного устройства

## 🚀 Быстрый старт

1. **Запустите сервер:**
   ```bash
   npm start
   ```

2. **Скопируйте IP-адрес**, который покажет сервер (например: `http://192.168.1.100:3000`)

3. **На iPhone/iPad:**
   - Откройте Safari
   - Введите адрес в адресной строке
   - Нажмите "Перейти"

## 🔍 Диагностика проблем

Если Safari не открывает сайт, запустите диагностику:

```bash
npm run check:network
```

Скрипт проверит:
- ✅ Наличие сетевых интерфейсов
- ✅ Доступность порта 3000
- ✅ Правильность IP-адресов

## 🔧 Решение проблем

### Проблема 1: Safari показывает "Не удается подключиться к серверу"

**Решение:**

1. **Проверьте брандмауэр Windows:**
   - Откройте "Панель управления" → "Система и безопасность" → "Брандмауэр Защитника Windows"
   - Нажмите "Дополнительные параметры"
   - Выберите "Правила для входящих подключений" → "Создать правило"
   - Выберите "Порт" → "TCP" → "Определенные локальные порты: 3000"
   - Выберите "Разрешить подключение"
   - Примените правило

2. **Альтернативный способ через PowerShell (от имени администратора):**
   ```powershell
   New-NetFirewallRule -DisplayName "Node.js Server" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
   ```

### Проблема 2: Устройство не видит сервер

**Проверьте:**

1. ✅ Оба устройства в одной Wi-Fi сети
2. ✅ IP-адрес правильный (не localhost, а реальный IP)
3. ✅ Сервер запущен (`npm start`)
4. ✅ Порт 3000 не заблокирован

**Проверка подключения:**
- На iPhone откройте Настройки → Wi-Fi
- Нажмите на вашу сеть
- Проверьте IP-адрес iPhone (должен быть в той же подсети, например: 192.168.1.XXX)

### Проблема 3: Safari блокирует HTTP

**Решение:**

Safari на iOS может блокировать небезопасные HTTP соединения. Попробуйте:

1. **Очистить кэш Safari:**
   - Настройки → Safari → Очистить историю и данные веб-сайтов

2. **Проверить настройки Safari:**
   - Настройки → Safari → Отключить "Блокировать все файлы cookie" (временно)

3. **Использовать Chrome на iPhone** для проверки (если Chrome работает, проблема в Safari)

### Проблема 4: Роутер блокирует подключения

Некоторые роутеры имеют "изоляцию клиентов" (AP Isolation), которая блокирует связь между устройствами в сети.

**Решение:**

1. Зайдите в настройки роутера (обычно `192.168.1.1` или `192.168.0.1`)
2. Найдите настройку "AP Isolation" или "Изоляция клиентов"
3. Отключите её
4. Перезагрузите роутер

## 🌐 Альтернативные решения

### Вариант 1: Использовать ngrok (туннель)

ngrok создает безопасный туннель к вашему локальному серверу.

1. **Установите ngrok:**
   ```bash
   npm install -g ngrok
   ```
   Или скачайте с [ngrok.com](https://ngrok.com)

2. **Запустите сервер:**
   ```bash
   npm start
   ```

3. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 3000
   ```

4. **Скопируйте URL** (например: `https://abc123.ngrok.io`)

5. **Откройте этот URL на iPhone** - будет работать через интернет!

### Вариант 2: Изменить порт

Если порт 3000 заблокирован, измените его:

1. Откройте `scripts/server.js`
2. Измените `const PORT = 3000;` на `const PORT = 8080;`
3. Перезапустите сервер
4. Используйте новый порт: `http://ВАШ_IP:8080`

### Вариант 3: Использовать локальный хостинг

Загрузите сайт на GitHub Pages, Netlify или Vercel для доступа с любого устройства.

## 📋 Чек-лист для Safari

- [ ] Сервер запущен (`npm start`)
- [ ] IP-адрес скопирован правильно
- [ ] iPhone в той же Wi-Fi сети
- [ ] Брандмауэр Windows разрешает порт 3000
- [ ] Используется IP-адрес, а не localhost
- [ ] Пробовали Chrome на iPhone (для проверки)
- [ ] Очистили кэш Safari
- [ ] Проверили настройки роутера (AP Isolation)

## 🆘 Если ничего не помогает

1. **Запустите диагностику:**
   ```bash
   npm run check:network
   ```

2. **Попробуйте ngrok** (самый надежный способ)

3. **Используйте другой браузер** на iPhone (Chrome, Firefox)

4. **Проверьте логи сервера** - возможно, там есть ошибки

5. **Попробуйте с другого устройства** (Android, другой iPhone) для проверки

## 💡 Полезные команды

```bash
# Запуск сервера
npm start

# Диагностика сети
npm run check:network

# Проверка IP-адреса (Windows)
ipconfig

# Проверка IP-адреса (Mac/Linux)
ifconfig
# или
ip addr
```
