# 🔍 Диагностика проблем с отправкой в Telegram

## Как проверить логи

### 1. Запустите сервер с логами

```bash
npm start
```

### 2. Отправьте тестовую заявку через форму

### 3. Проверьте логи в консоли

Вы должны увидеть последовательность сообщений:

```
📨 Получен запрос на отправку заявки
📋 Данные заявки: {...}
📤 Отправка уведомления в Telegram...
🔧 Инициализация бота для уведомлений...
✅ Бот инициализирован
👥 Найдено администраторов: 1
   ID администраторов: 123456789
📝 Сформировано сообщение:
🎯 📋 Заявка на бесплатный проект
...
📤 Отправка сообщений администраторам...
✅ Уведомление отправлено администратору 123456789
✅ Уведомление отправлено 1 из 1 администраторам
✅ Заявка успешно обработана
```

## Возможные проблемы и решения

### ❌ "TELEGRAM_BOT_TOKEN не установлен"

**Причина:** Переменная окружения не загружена

**Решение:**
1. Проверьте наличие файла `.env` в корне проекта
2. Убедитесь, что в файле есть строка: `TELEGRAM_BOT_TOKEN=ваш_токен`
3. Проверьте, что токен правильный (формат: `123456:ABC-DEF...`)

### ❌ "ADMIN_IDS не установлен"

**Причина:** ID администратора не указан

**Решение:**
1. Добавьте в `.env`: `ADMIN_IDS=ваш_telegram_id`
2. Чтобы узнать ID, напишите боту @userinfobot в Telegram
3. Можно указать несколько ID через запятую: `ADMIN_IDS=123456,789012`

### ❌ "Ошибка отправки уведомления администратору"

**Возможные причины:**

1. **Бот не запущен**
   - Запустите: `npm run bot`
   - Бот должен быть активен для отправки сообщений

2. **Администратор не написал боту /start**
   - Откройте бота в Telegram
   - Отправьте команду `/start`
   - Бот должен ответить приветствием

3. **Неверный Telegram ID**
   - Проверьте ID через @userinfobot
   - Убедитесь, что ID в `.env` правильный (только цифры)

4. **Проблемы с сетью**
   - Проверьте интернет-соединение
   - Убедитесь, что Telegram доступен

### ❌ "Код ошибки: 403"

**Причина:** Бот заблокирован пользователем или не имеет доступа

**Решение:**
- Администратор должен разблокировать бота
- Отправить боту `/start`

### ❌ "Код ошибки: 400"

**Причина:** Неверный формат запроса

**Решение:**
- Проверьте формат токена
- Убедитесь, что бот активен

## Тестирование вручную

### Проверка переменных окружения

Создайте файл `test-env.js`:

```javascript
require('dotenv').config();
console.log('TELEGRAM_BOT_TOKEN:', process.env.TELEGRAM_BOT_TOKEN ? '✅ Загружен' : '❌ Не найден');
console.log('ADMIN_IDS:', process.env.ADMIN_IDS || '❌ Не найден');
```

Запустите: `node test-env.js`

### Тест отправки сообщения

Создайте файл `test-send.js`:

```javascript
const { sendFormNotification } = require('./telegram-bot/utils/notifications');

const testData = {
  name: 'Тестовый пользователь',
  phone: '+7 (900) 123-45-67',
  area: '120',
  formType: 'CTA'
};

sendFormNotification(testData)
  .then(success => {
    console.log('Результат:', success ? '✅ Успешно' : '❌ Ошибка');
    process.exit(success ? 0 : 1);
  })
  .catch(error => {
    console.error('Ошибка:', error);
    process.exit(1);
  });
```

Запустите: `node test-send.js`

## Чек-лист проверки

- [ ] Файл `.env` существует в корне проекта
- [ ] `TELEGRAM_BOT_TOKEN` указан в `.env`
- [ ] `ADMIN_IDS` указан в `.env`
- [ ] Токен правильного формата (число:буквы)
- [ ] Telegram ID правильный (только цифры)
- [ ] Бот запущен (`npm run bot`)
- [ ] Администратор написал боту `/start`
- [ ] Сервер запущен (`npm start`)
- [ ] Нет ошибок в консоли сервера
- [ ] Нет ошибок в консоли бота

## Получение подробных логов

Все операции логируются в консоль. Если сообщения не приходят:

1. Проверьте консоль сервера (где запущен `npm start`)
2. Проверьте консоль бота (где запущен `npm run bot`)
3. Ищите сообщения с эмодзи ❌ или ⚠️
