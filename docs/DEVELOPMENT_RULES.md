# Правила разработки сайта

## 🎯 Общие принципы

### Код
- **Чистый код**: Используйте понятные имена переменных и функций
- **Модульность**: Разделяйте код на логические модули
- **DRY принцип**: Не повторяйте код, выносите общую логику в утилиты
- **Документирование**: Комментируйте сложную логику и нестандартные решения

### Архитектура
- **Разделение ответственности**: HTML для структуры, CSS для стилей, JS для логики
- **Mobile-first**: Адаптивность начинается с мобильных устройств
- **Прогрессивное улучшение**: Базовая функциональность работает без JS
- **Производительность**: Оптимизируйте изображения, минифицируйте код

## 📁 Структура проекта

```
/
├── src/                    # Исходные файлы
│   ├── css/               # CSS файлы для разработки
│   ├── js/                # JavaScript модули
│   ├── images/            # Исходные изображения
│   └── components/        # Переиспользуемые компоненты
├── public/                # Готовые файлы для продакшена
│   ├── css/              # Скомпилированные CSS
│   ├── js/               # Скомпилированный JS
│   ├── images/           # Оптимизированные изображения
│   └── index.html        # Главная страница
├── docs/                 # Документация
├── tests/                # Тесты
└── package.json          # Зависимости и скрипты
```

## 🎨 CSS правила

### Именование классов (BEM методология)
- **Блок**: `.nav`, `.hero`, `.card`
- **Элемент**: `.nav__menu`, `.hero__title`, `.card__content`
- **Модификатор**: `.btn--primary`, `.card--large`

### Организация CSS
- Используйте CSS переменные для цветов, отступов, шрифтов
- Группируйте стили логически: общие → компоненты → утилиты
- Используйте mobile-first медиа запросы

### Примеры
```css
/* ❌ Плохо */
.redButton { background: red; }

/* ✅ Хорошо */
.btn--danger { background-color: var(--danger-color); }
```

## 📜 JavaScript правила

### Стиль кода
- Используйте `const` и `let`, избегайте `var`
- Предпочитайте стрелочные функции для коллбеков
- Используйте современный ES6+ синтаксис
- Обрабатывайте ошибки с помощью try/catch

### Организация
- Один модуль = один файл
- Экспортируйте только необходимые функции
- Используйте семантичные имена функций и переменных

### Примеры
```javascript
// ❌ Плохо
var btn = document.getElementById('btn');
btn.onclick = function() { alert('Привет'); }

// ✅ Хорошо
const button = document.querySelector('.btn--primary');
button.addEventListener('click', () => {
    showNotification('Добро пожаловать!', 'success');
});
```

## 🔧 Инструменты разработки

### Команды npm
- `npm start` - Запустить локальный сервер для разработки
- `npm run dev` - Разработка с автоперезагрузкой
- `npm run build` - Сборка для продакшена
- `npm run watch` - Наблюдение за изменениями файлов
- `npm run lint` - Проверка кода на ошибки
- `npm run format` - Форматирование кода
- `npm test` - Запуск тестов

### Workflow
1. Создайте новую ветку для каждой фичи: `git checkout -b feature/название`
2. Разрабатывайте с `npm run dev`
3. Тестируйте с `npm test`
4. Проверяйте код с `npm run lint`
5. Собирайте для продакшена с `npm run build`

## 🎯 Стандарты качества

### Performance
- Изображения: WebP формат, оптимизированные размеры
- CSS: Минимизируйте unused CSS, используйте critical CSS
- JavaScript: Ленивая загрузка, code splitting
- Шрифты: Используйте font-display: swap

### Accessibility (A11y)
- Семантичная HTML разметка
- Alt текст для изображений
- Контрастность цветов минимум 4.5:1
- Поддержка навигации с клавиатуры

### SEO
- Мета теги в каждой странице
- Структурированная разметка (JSON-LD)
- Правильная иерархия заголовков (h1-h6)
- Оптимизированные URL

## 🔒 Безопасность

- Валидируйте все пользовательские вводы
- Используйте HTTPS для продакшена
- Не храните секреты в клиентском коде
- Настройте Content Security Policy (CSP)

## 🧪 Тестирование

### Типы тестов
- **Unit тесты**: Для отдельных функций и модулей
- **Integration тесты**: Для взаимодействия компонентов
- **E2E тесты**: Для полных пользовательских сценариев

### Покрытие
- Стремитесь к покрытию минимум 80%
- Тестируйте критически важную функциональность

## 📋 Code Review

### Чек-лист для PR
- [ ] Код соответствует стандартам проекта
- [ ] Добавлены необходимые тесты
- [ ] Обновлена документация (если нужно)
- [ ] Проверена производительность
- [ ] Проверена доступность

## 🚀 Деплой

### Перед деплоем
1. Запустить все тесты: `npm test`
2. Собрать проект: `npm run build`
3. Проверить линтер: `npm run lint`
4. Проверить в разных браузерах

## 📚 Дополнительные правила

### Git
- Используйте понятные commit сообщения
- Делайте атомарные коммиты
- Используйте branching strategy (Git Flow или GitHub Flow)

### Файлы
- Используйте kebab-case для имён файлов
- Организуйте файлы по функциональности, не по типу
- Удаляйте неиспользуемые файлы и зависимости

---

**Примечание**: Эти правила можно дополнять и изменять по мере развития проекта. Регулярно пересматривайте и обновляйте их.