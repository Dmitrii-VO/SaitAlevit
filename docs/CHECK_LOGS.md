# 🔍 Как проверить логи отправки в Telegram

## Быстрая проверка

### Шаг 1: Проверьте переменные окружения

```bash
npm run test:env
```

Этот скрипт проверит:
- ✅ Наличие файла `.env`
- ✅ Наличие `TELEGRAM_BOT_TOKEN`
- ✅ Наличие `ADMIN_IDS`
- ✅ Правильность формата токена и ID

**Ожидаемый результат:**
```
✅ Все переменные настроены правильно!
```

### Шаг 2: Тест отправки уведомления

```bash
npm run test:telegram
```

Этот скрипт попытается отправить тестовое сообщение в Telegram.

**Ожидаемый результат:**
```
✅ ТЕСТ ПРОЙДЕН: Уведомление успешно отправлено!
   Проверьте Telegram - сообщение должно прийти.
```

## Проверка логов при работе сервера

### 1. Запустите сервер

```bash
npm start
```

### 2. Откройте сайт и отправьте форму

### 3. Смотрите логи в консоли

Вы должны увидеть последовательность:

```
📨 Получен запрос на отправку заявки
📋 Данные заявки: {
  "name": "Иван Иванов",
  "phone": "+7 (900) 123-45-67",
  "area": "120",
  "formType": "CTA"
}
📤 Отправка уведомления в Telegram...
📤 Отправка уведомления в Telegram...
   Администраторов: 1
   ID: 123456789
✅ Уведомление отправлено администратору 123456789
✅ Уведомление отправлено 1 из 1 администраторам
✅ Заявка успешно обработана
```

## Типичные ошибки в логах

### ❌ "TELEGRAM_BOT_TOKEN не установлен"

**Что делать:**
1. Проверьте файл `.env` в корне проекта
2. Убедитесь, что есть строка: `TELEGRAM_BOT_TOKEN=ваш_токен`
3. Запустите: `npm run test:env`

### ❌ "ADMIN_IDS не установлен или пуст"

**Что делать:**
1. Добавьте в `.env`: `ADMIN_IDS=ваш_telegram_id`
2. Чтобы узнать ID, напишите боту @userinfobot
3. Можно несколько ID: `ADMIN_IDS=123456,789012`

### ❌ "Ошибка отправки администратору: Код: 403"

**Причина:** Администратор не написал боту `/start`

**Решение:**
1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветствием
4. Попробуйте отправить форму снова

### ❌ "Ошибка отправки администратору: Код: 400"

**Причина:** Неверный формат данных

**Решение:**
1. Проверьте формат токена (должен быть: `123456:ABC-DEF...`)
2. Проверьте Telegram ID (должны быть только цифры)
3. Запустите: `npm run test:env`

### ❌ "Не удалось отправить уведомление ни одному администратору"

**Возможные причины:**
1. Администратор не написал боту `/start` ← **Самая частая причина!**
2. Неверный Telegram ID
3. Проблемы с сетью

**Решение:**
1. Убедитесь, что вы написали боту `/start`
2. Проверьте ID через @userinfobot
3. Проверьте интернет-соединение

## Полная диагностика

Если ничего не помогает, выполните по порядку:

```bash
# 1. Проверка переменных
npm run test:env

# 2. Тест отправки
npm run test:telegram

# 3. Запуск сервера с логами
npm start

# 4. В другом терминале - запуск бота (если нужно)
npm run bot
```

## Что проверить вручную

1. **Файл `.env` существует?**
   ```bash
   ls .env
   # или в Windows:
   dir .env
   ```

2. **Содержимое `.env` правильное?**
   ```env
   TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ADMIN_IDS=123456789
   ```

3. **Бот отвечает?**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Должен ответить

4. **ID правильный?**
   - Напишите @userinfobot
   - Скопируйте ваш ID
   - Сравните с `.env`

## Полезные команды

```bash
# Проверка переменных окружения
npm run test:env

# Тест отправки в Telegram
npm run test:telegram

# Запуск сервера
npm start

# Запуск бота
npm run bot
```

---

**💡 Совет:** Если логи показывают ошибку, скопируйте полный вывод и проверьте раздел "Типичные ошибки" выше.
