# Telegram бот для администрирования сайта АЛЕВИТ СТРОЙ

Telegram бот для управления контентом сайта через удобный интерфейс в мессенджере.

## 🎯 Возможности

- ✅ **Управление проектами** - добавление, редактирование, удаление типовых проектов домов
- ✅ **Управление работами** - управление портфолио построенных домов
- ✅ **Управление отзывами** - добавление отзывов клиентов (текстовые и видео)
- ✅ **Управление контактами** - редактирование контактной информации
- ✅ **Загрузка изображений** - поддержка загрузки фото прямо в чат
- ✅ **Безопасность** - авторизация по Telegram ID, только для администраторов
- ✅ **Статусы публикации** - возможность скрывать элементы без удаления

## 📋 Требования

- Node.js 14+ 
- npm или yarn
- Telegram аккаунт
- Токен Telegram бота (получить у @BotFather)

## 🚀 Установка

1. **Установите зависимости:**

```bash
npm install
```

2. **Создайте файл `.env` в корне проекта:**

```env
# Токен Telegram бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID администраторов через запятую (можно несколько)
# Чтобы узнать свой ID, напишите боту @userinfobot в Telegram
ADMIN_IDS=123456789,987654321
```

3. **Получите токен бота:**

- Найдите бота [@BotFather](https://t.me/botfather) в Telegram
- Отправьте команду `/newbot`
- Следуйте инструкциям для создания бота
- Скопируйте полученный токен в `.env` файл

4. **Узнайте свой Telegram ID:**

- Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
- Отправьте команду `/start`
- Скопируйте ваш ID (число) в `.env` файл в поле `ADMIN_IDS`

## 🏃 Запуск

### Запуск бота:

```bash
npm run bot
```

### Запуск в режиме разработки (с автоперезагрузкой):

```bash
npm run bot:dev
```

## 📖 Использование

После запуска бота, откройте его в Telegram и отправьте команду `/start`.

### Основные команды:

- `/start` или `/menu` - главное меню
- `/help` - справка по использованию
- `/cancel` - отменить текущую операцию

### Управление проектами:

- `/projects_add` - добавить новый проект
- `/projects_list` - список всех проектов
- `/projects_edit` - редактировать проект (название, этажность, площадь, стоимость, описание, статус, главное фото, галерея)
- `/projects_delete` - удалить проект

### Управление работами:

- `/works_add` - добавить новую работу
- `/works_list` - список всех работ
- `/works_edit` - редактировать работу (название, площадь, формат, статус работы, описание, адрес, фото, галерея, статус публикации)
- `/works_delete` - удалить работу

### Управление отзывами:

- `/reviews_add` - добавить новый отзыв
- `/reviews_list` - список всех отзывов
- `/reviews_edit` - редактировать отзыв (имя, название дома, текст, тип, видео/фото, статус)
- `/reviews_delete` - удалить отзыв

### Управление контактами:

- `/contacts_view` - просмотр текущих контактов
- `/contacts_edit` - редактировать контакты

## 📝 Примеры использования

### Добавление проекта:

1. Отправьте `/projects_add`
2. Введите название проекта
3. Выберите этажность
4. Введите площадь
5. Введите стоимость
6. Введите описание (или пропустите)
7. Отправьте главное фото
8. Отправьте фото для галереи (можно несколько)
9. Отправьте `/done` для завершения галереи
10. Выберите статус (опубликован/скрыт)

### Добавление отзыва:

1. Отправьте `/reviews_add`
2. Введите имя клиента
3. Введите название дома (или пропустите)
4. Введите текст отзыва
5. Выберите тип (видео/текст)
6. Если видео - введите ссылку
7. Если текст - отправьте фото (или пропустите)
8. Выберите статус публикации

## 📁 Структура данных

Все данные сохраняются в JSON файлах в папке `data/`:

- `data/projects.json` - типовые проекты домов
- `data/works.json` - портфолио работ
- `data/reviews.json` - отзывы клиентов
- `data/contacts.json` - контактная информация

Изображения сохраняются в папке `images/`:

- `images/projects/` - фото проектов
- `images/works/` - фото работ
- `images/reviews/` - фото для отзывов

## 🔒 Безопасность

- Доступ к боту имеют только пользователи, ID которых указаны в `ADMIN_IDS`
- Все данные сохраняются локально в JSON файлах
- Токен бота хранится в `.env` файле (не коммитится в Git)

## ⚙️ Конфигурация

Конфигурация находится в файле `telegram-bot/config.js`:

- `botToken` - токен бота из `.env`
- `adminIds` - массив ID администраторов
- `dataPaths` - пути к JSON файлам
- `imagesPath` - путь для сохранения изображений
- `maxFileSize` - максимальный размер файла (10 МБ)
- `allowedImageTypes` - разрешенные типы изображений

## 🐛 Решение проблем

### Бот не отвечает:

1. Проверьте, что токен указан правильно в `.env`
2. Убедитесь, что ваш ID добавлен в `ADMIN_IDS`
3. Проверьте логи на наличие ошибок

### Ошибка при загрузке фото:

1. Проверьте размер файла (максимум 10 МБ)
2. Убедитесь, что формат изображения поддерживается (JPEG, PNG, WebP)
3. Проверьте права на запись в папку `images/`

### Ошибка при сохранении данных:

1. Убедитесь, что папка `data/` существует
2. Проверьте права на запись в папку `data/`
3. Убедитесь, что JSON файлы не повреждены

## 📚 Структура проекта

```
telegram-bot/
├── bot.js              # Основной файл бота
├── config.js           # Конфигурация
├── handlers/           # Обработчики команд
│   ├── projects.js     # Управление проектами
│   ├── works.js        # Управление работами
│   ├── reviews.js      # Управление отзывами
│   └── contacts.js     # Управление контактами
├── utils/              # Утилиты
│   ├── auth.js         # Авторизация
│   ├── data-manager.js # Работа с JSON
│   └── file-manager.js # Работа с файлами
└── README.md           # Эта документация
```

## 🔄 Интеграция с сайтом

После изменений через бота, данные сохраняются в JSON файлы. Для отображения на сайте:

1. Используйте JavaScript для загрузки данных из JSON
2. Рендерите карточки динамически на основе данных из JSON
3. Используйте пути к изображениям из JSON для отображения фото

## 📝 Примечания

- Бот должен работать постоянно для обработки команд
- Рекомендуется использовать процесс-менеджер (PM2, forever) для продакшена
- Для тестирования можно использовать ngrok для туннелирования локального сервера
- Все изменения в данных сохраняются сразу в JSON файлы

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию в `.env`
4. Убедитесь, что структура папок создана правильно

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024
