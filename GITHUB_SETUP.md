# 📤 Инструкция по загрузке проекта на GitHub

## ✅ Что уже сделано

1. ✅ Git репозиторий инициализирован
2. ✅ Все файлы добавлены в staging area
3. ✅ Создан первый коммит
4. ✅ Создан README.md

## 🚀 Следующие шаги

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте [GitHub.com](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name:** `saitgusia` (или другое название)
   - **Description:** "Лендинг строительной компании АЛЕВИТ СТРОЙ"
   - **Visibility:** выберите Public или Private
   - **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license" (у нас уже всё есть)
4. Нажмите **"Create repository"**

### Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/Dmitrii-VO/SaitAlevit.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Или если используете SSH:**

```bash
git remote add origin git@github.com:Dmitrii-VO/SaitAlevit.git
git branch -M main
git push -u origin main
```

### Шаг 3: Проверьте результат

Откройте ваш репозиторий на GitHub — все файлы должны быть загружены.

## 📝 Дальнейшая работа

После первого push, для обновления кода используйте:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "feat: описание изменений"

# Отправить на GitHub
git push
```

## 🔐 Если нужна аутентификация

### Вариант 1: Personal Access Token (рекомендуется)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен вместо пароля

### Вариант 2: SSH ключ

1. Сгенерируйте SSH ключ: `ssh-keygen -t ed25519 -C "your_email@example.com"`
2. Добавьте ключ в GitHub: Settings → SSH and GPG keys → New SSH key
3. Используйте SSH URL для remote: `git@github.com:USERNAME/REPO.git`

## ⚠️ Важные замечания

- **Не коммитьте** файлы с секретами (API ключи, пароли)
- **Проверьте** `.gitignore` — он должен исключать `node_modules/`, `.env` и другие временные файлы
- **Используйте** осмысленные сообщения коммитов (формат Conventional Commits)

## 🆘 Если что-то пошло не так

### Ошибка "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/saitgusia.git
```

### Ошибка аутентификации
- Проверьте правильность username и токена
- Убедитесь, что репозиторий существует на GitHub

### Отменить последний коммит (если нужно)
```bash
git reset --soft HEAD~1  # Отменить коммит, сохранив изменения
```

---

**Готово!** Ваш проект теперь на GitHub 🎉

