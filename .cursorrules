# Правила разработки для проекта АЛЕВИТ СТРОЙ

> **⚠️ ВАЖНО:** Если есть файл `PROMPT.md` — промпт из него имеет приоритет над этими правилами. При наличии противоречий следуй промпту.

## 🎯 Контекст проекта

**Проект:** Продающий лендинг для строительной компании ООО «АЛЕВИТ СТРОЙ»
**Сфера:** Строительство частных домов под ключ
**Регион:** Белгородская область (г. Белгород и Белгородский район)
**Цель:** Получение целевых заявок на строительство, расчёт стоимости, консультации

## 👨‍💻 Роль разработчика

Ты — Senior Web Developer & Web Architect с 10+ лет опыта. Эксперт в:
- HTML5, CSS3, JavaScript (ES6+), TypeScript
- Современных фреймворках (React, Next.js, Vue)
- Backend (Node.js, REST API)
- Адаптивная верстка, SEO, Web Performance
- Accessibility (A11y) и UX best practices

## 📋 Технический стек

**Frontend:**
- Чистый HTML/CSS/JavaScript (без фреймворков для лендинга)
- PostCSS с автопрефиксером
- Webpack для сборки
- Mobile-first подход

**Структура проекта:**
- `/src` - исходные файлы для разработки
- `/public` - готовые файлы для продакшена
- `/docs` - документация
- `/tests` - тесты

## 🎨 Дизайн-требования

### Цветовая палитра
- **Основные:** чёрный (#000000, #1a1a1a) + золото (#D4AF37, #B8860B)
- **Допустимые:** белый (#FFFFFF), серый (#F5F5F5, #808080, #333333)
- **Запрещены:** яркие цвета, кричащие оттенки

### Визуальный стиль
- Современный, строгий, премиальный
- Минимализм
- Ассоциации: уверенность, надёжность, уют
- Пространство и воздух в дизайне

### Изображения
- **ТОЛЬКО реальные фото** построенных домов
- **НЕ использовать:** AI-рендеры, стоковые изображения домов
- Допускаются фото команды и руководителей

## 🏗️ Структура лендинга (13 секций)

1. **Первый экран (Hero)**
   - Заголовок с акцентом на доверие
   - Подзаголовок (опыт 6+ лет, сроки, гарантия 5 лет)
   - 2 CTA: "Рассчитать стоимость", "Получить бесплатный проект"

2. **Блок доверия**
   - 6+ лет опыта
   - 60+ построенных домов
   - Гарантия 5 лет
   - Фиксированная цена по договору

3. **Почему выбирают нас**
   - Надёжность
   - Сроки
   - Контроль качества
   - Финансовая прозрачность

4. **Форматы домов и цены**
   - Типы домов (материалы: газобетон, кирпич, каркас и т.д.)
   - Цена за м² (коробка, чистовая, под ключ)
   - Диапазон "от ..."
   - Полная стоимость через калькулятор/заявку

5. **Проекты**
   - Типовые проекты домов
   - Каталог с фото и описанием

6. **Наши работы**
   - Реальные построенные дома
   - Фото + краткое описание

7. **Этапы строительства**
   - Пошаговый процесс от консультации до сдачи
   - Визуальная временная линия

8. **Карта объектов**
   - Интерактивная карта-схема
   - Цвета: 🟢 построен и продаётся, 🟠 строится, 🔴 продан

9. **Услуги**
   - Ипотека
   - Подбор участка
   - Проектирование

10. **Отзывы**
    - Видео отзывы (3 шт.)
    - Текстовые отзывы

11. **О компании / Команда**
    - Руководители
    - Опыт компании
    - Философия компании

12. **CTA + Формы**
    - Рассчитать стоимость
    - Получить проект
    - Консультация

13. **Контакты**
    - Телефон, Email
    - Мессенджеры (WhatsApp, Telegram)
    - Адрес: Белгородская область, Белгородский район, с. Пушкарное-78, ул. Кузнечная, д. 9

## 💻 Правила кода

### HTML
- Семантичная разметка (header, nav, main, section, article, footer)
- Правильная иерархия заголовков (h1-h6)
- Accessibility атрибуты (aria-labels, roles при необходимости)

### CSS (BEM методология)
- Блок: `.hero`, `.trust-block`, `.calculator`
- Элемент: `.hero__title`, `.trust-block__item`
- Модификатор: `.btn--primary`, `.btn--gold`, `.card--featured`
- CSS переменные для цветов, отступов, шрифтов
- Использование `rem` для размеров

### JavaScript
- ES6+ синтаксис (const, let, arrow functions, destructuring)
- Модульная архитектура
- Обработка ошибок (try/catch)
- Плавная прокрутка для якорных ссылок

### Контроль размера кода
Соблюдай лимиты для читаемости и поддержки:
- **Функция** — до ~30 строк
- **Класс** — до ~400 строк
- **Файл** — до ~800 строк
- **Строка** — до ~120 символов

Если код превышает лимиты:
1. Разбей функцию на несколько более мелких
2. Вынеси логику класса в отдельные модули или миксины
3. Раздели файл на несколько модулей с четкими обязанностями
4. Перенеси часть строки на следующую строку или упрости выражение

### Именование переменных и функций
- **Переменные и функции:** camelCase (`calculatePrice`, `userName`)
- **Константы:** UPPER_SNAKE_CASE (`MAX_PRICE`, `API_ENDPOINT`)
- **Классы:** PascalCase (`Calculator`, `FormValidator`)
- **CSS классы:** BEM (`.block__element--modifier`)
- **Файлы:** kebab-case (`form-validator.js`, `price-calculator.js`)
- **Используй семантичные имена:** `calculateHousePrice()` вместо `calc()`
- **Булевы переменные:** префикс `is`, `has`, `should` (`isValid`, `hasError`)

### Комментарии и документация
- **JSDoc** для всех функций и классов:
```javascript
/**
 * Рассчитывает стоимость дома на основе параметров
 * @param {number} area - Площадь дома в м²
 * @param {string} houseType - Тип дома (газобетон, кирпич, каркас)
 * @param {string} finishType - Тип отделки (коробка, чистовая, под ключ)
 * @returns {number} Рассчитанная стоимость в рублях
 */
```
- **Комментируй сложную логику**, не очевидные решения
- **Не комментируй** очевидный код
- **HTML комментарии** только для разметки структуры сложных секций
- **CSS комментарии** для группировки стилей и объяснения нестандартных решений

### Обработка ошибок
- Всегда используй `try/catch` для асинхронных операций
- Обрабатывай ошибки пользовательски-дружелюбно
- Логируй ошибки в консоль для разработки
- Показывай понятные сообщения пользователю
- Не выбрасывай ошибки в UI, обрабатывай их gracefully

### Асинхронный код
- Используй `async/await` вместо промисов с `.then()`
- Обрабатывай ошибки с `try/catch`
- Используй `Promise.all()` для параллельных запросов
- Добавляй таймауты для сетевых запросов (30 сек по умолчанию)

### Состояния UI
Реализуй состояния для всех интерактивных элементов:
- **Loading:** показывай индикатор загрузки
- **Success:** сообщение об успехе, затем возврат к исходному состоянию
- **Error:** показывай понятное сообщение об ошибке
- **Disabled:** блокируй повторные отправки форм
- Используй атрибуты `aria-busy`, `aria-live` для accessibility

### Валидация форм
- **Валидация на клиенте** перед отправкой
- **Валидация в реальном времени** для улучшения UX
- **Правила валидации:**
  - Телефон: формат +7 (XXX) XXX-XX-XX или 8 XXX XXX-XX-XX
  - Email: стандартная валидация email
  - Имя: минимум 2 символа, только буквы и пробелы
  - Площадь: положительное число, разумный диапазон (20-500 м²)
- **Показывай ошибки** рядом с полями
- **Подсвечивай невалидные поля** красной рамкой
- **Блокируй отправку** при наличии ошибок

### Работа с изображениями
- **Форматы:** WebP с fallback на JPG/PNG
- **Lazy loading:** `loading="lazy"` для всех изображений ниже fold
- **Responsive images:** используй `srcset` и `sizes`
- **Alt-тексты:** обязательны, описательные (не "изображение", а "Дом из газобетона, 120 м²")
- **Размеры:** указывай `width` и `height` для предотвращения layout shift
- **Оптимизация:** сжимай изображения перед использованием

### Форматирование данных
- **Цены:** форматируй с пробелами (1 250 000 ₽)
- **Даты:** формат для России (ДД.ММ.ГГГГ)
- **Номера телефонов:** единый формат +7 (XXX) XXX-XX-XX
- Используй `Intl.NumberFormat` и `Intl.DateTimeFormat` для форматирования

### API и сетевые запросы
- Используй `fetch` API
- Всегда обрабатывай ошибки сетевых запросов
- Добавляй таймауты (30 секунд)
- Показывай состояние загрузки
- Используй заголовки для Content-Type
- Валидируй ответы сервера перед использованием

### Кроссбраузерность
Поддерживаемые браузеры:
- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)
- Мобильные браузеры (iOS Safari, Chrome Mobile)
- Используй автопрефиксер для CSS
- Проверяй совместимость JavaScript функций
- Используй полифиллы при необходимости

### Accessibility (A11y)
- **ARIA атрибуты:** `aria-label`, `aria-labelledby`, `aria-describedby`
- **Навигация с клавиатуры:** Tab, Enter, Escape работают корректно
- **Фокус:** видимый индикатор фокуса для всех интерактивных элементов
- **Контрастность:** минимум 4.5:1 для текста, 3:1 для UI элементов
- **Skip links:** для пропуска навигации
- **Формы:** связывай labels с inputs через `for` и `id`

## 📝 Формы и калькулятор

### Формы
- Минимум: имя + телефон
- Расширенные: площадь, тип дома, формат отделки
- Согласие на обработку персональных данных (обязательно)
- Политика конфиденциальности (ссылка)

### Калькулятор стоимости
- Параметры:
  - Площадь дома (м²)
  - Тип дома (газобетон, кирпич, каркас и т.д.)
  - Формат отделки (коробка, чистовая, под ключ)
- Результат:
  - Примерная цена (диапазон)
  - Полная стоимость после отправки заявки

## 🔧 Функциональные требования

### Обязательные функции
- Калькулятор стоимости домов
- Интерактивная карта объектов
- Формы заявок с валидацией
- Плавная прокрутка
- Анимации при скролле (Intersection Observer)
- Мобильное меню (бургер)

### SEO и Performance
- SEO-базовая оптимизация
- Meta-теги (title, description, keywords)
- Структурированные данные (JSON-LD)
- Open Graph теги
- Быстрая загрузка (< 3 сек)
- Минификация CSS/JS

## 📱 Адаптивность

- **Mobile-first подход** (начинай с мобильных устройств)
- **Брейкпоинты:**
  - Mobile: до 768px
  - Tablet: 768px - 1024px
  - Desktop: 1024px+
- Все элементы должны корректно отображаться на всех устройствах
- Touch-friendly интерфейс для мобильных (минимальный размер кликабельной области 44x44px)

## 🚫 Что НЕ делать

- Не использовать яркие цвета
- Не добавлять AI-рендеры домов
- Не использовать устаревшие технологии (jQuery, старый ES5)
- Не создавать тяжёлые анимации
- Не нарушать семантику HTML
- Не игнорировать accessibility

## ✅ Что делать обязательно

- Писать чистый, читаемый код с комментариями
- Использовать современные стандарты (ES6+, semantic HTML)
- Оптимизировать производительность
- Обеспечить доступность (A11y)
- Проверять кроссбраузерность
- Валидировать HTML/CSS
- Тестировать на разных устройствах

## 🎯 УТП и позиционирование

**УТП:** Дом под ключ от 3 месяцев с фиксированной ценой и гарантией 5 лет. Проект — бесплатно.

**Позиционирование:** Надёжная строительная компания с фиксированной ценой и гарантией.

**Целевая аудитория:**
- Семьи, молодые пары, люди 35+
- Основной страх: "Нас обманут, цена вырастет, качество будет плохим"

**Триггеры доверия:**
- Фиксированная цена
- Гарантия 5 лет
- Реальные фото домов
- Прозрачные этапы работ

## 📦 Команды разработки

```bash
npm start          # Локальный сервер (public)
npm run dev        # Разработка с автоперезагрузкой
npm run build      # Сборка для продакшена
npm run watch      # Наблюдение за изменениями
npm run lint       # Проверка кода
npm run format     # Форматирование
npm test           # Тесты
```

## 🔒 Безопасность и юридика

- Политика конфиденциальности — обязательна
- Согласие на обработку персональных данных — обязательно

## 📚 Дополнительные требования

- Код должен быть готов к масштабированию
- Компоненты переиспользуемые
- Легко поддерживаемый код
- Готовность к интеграции с CRM/backend API
- Возможность добавления контента через CMS в будущем

## 🔄 Git и версионирование

### Commit сообщения
Используй формат Conventional Commits:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, отступы (не влияет на код)
- `refactor:` - рефакторинг кода
- `perf:` - улучшение производительности
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, настройки

Пример: `feat: добавить калькулятор стоимости домов`

### Ветвление
- `main` - стабильная версия для продакшена
- `develop` - ветка для разработки
- `feature/название` - новая функциональность
- `fix/название` - исправление бага

## 🧪 Тестирование

- **Unit тесты:** для функций и модулей
- **Integration тесты:** для взаимодействия компонентов
- **E2E тесты:** для критических пользовательских сценариев
- Тестируй формы, калькулятор, валидацию
- Проверяй работу на разных устройствах
- Тестируй accessibility с помощью инструментов

## 📊 Производительность

### Метрики
- **First Contentful Paint (FCP):** < 1.8 сек
- **Largest Contentful Paint (LCP):** < 2.5 сек
- **Time to Interactive (TTI):** < 3.8 сек
- **Cumulative Layout Shift (CLS):** < 0.1
- **Total Blocking Time (TBT):** < 200 мс

### Оптимизации
- Минификация CSS и JS в продакшене
- Code splitting при необходимости
- Кэширование статических ресурсов
- Используй `defer` для скриптов в `<head>`

## 🔍 SEO детали

### Meta-теги
- **Title:** 50-60 символов, уникальный для каждой страницы
- **Description:** 150-160 символов, привлекательное описание
- **Keywords:** релевантные ключевые слова (но не переспам)
- **Open Graph:** для социальных сетей
- **Canonical URL:** предотвращение дублей

### Структурированные данные (JSON-LD)
- `LocalBusiness` - для компании
- `Organization` - для организации
- `Product` / `Service` - для услуг
- `Review` / `AggregateRating` - для отзывов
- `BreadcrumbList` - для навигации

### Технические SEO
- Правильная иерархия заголовков (один h1 на страницу)
- Внутренняя перелинковка

## 🛠️ Работа с кодом

### Импорты и экспорты
- Используй ES6 модули (`import`/`export`)
- Группируй импорты: внешние библиотеки → внутренние модули
- Используй именованные экспорты для утилит
- Дефолтный экспорт только для главного компонента модуля

### Деструктуризация
- Используй деструктуризацию для объектов и массивов
- Присваивай значения по умолчанию
- Используй rest/spread операторы где уместно

### Условные операторы
- Предпочитай тернарный оператор для простых условий
- Используй `&&` для условного рендеринга
- Избегай вложенных тернарных операторов (максимум один уровень)

## 📝 Логирование и отладка

- Используй `console.log()` только для разработки
- Удаляй все `console.log()` перед коммитом в продакшен
- Используй `console.error()` для ошибок
- Не логируй чувствительные данные (пароли, токены)
- В продакшене отправляй ошибки на сервер для мониторинга

## 🎨 Работа со стилями

### CSS переменные
Определяй все повторяющиеся значения как CSS переменные:
- Цвета (основные, дополнительные, состояния)
- Размеры шрифтов
- Отступы и размеры
- Переходы и анимации
- Тени и эффекты

### Анимации
- Используй CSS анимации вместо JavaScript где возможно
- Соблюдай правило: анимации должны быть быстрыми (200-300ms)
- Используй `will-change` осторожно, только для элементов с анимацией
- Предоставляй возможность отключить анимации (prefers-reduced-motion)

### Media queries
- Используй `min-width` для увеличения экрана
- Группируй стили по компонентам, а не по брейкпоинтам
- Используй контейнерные запросы где поддерживается

## 🔐 Безопасность

### XSS защита
- Экранируй пользовательский ввод перед выводом в HTML
- Используй `textContent` вместо `innerHTML` где возможно
- Валидируй и санитизируй данные с форм
- Используй Content Security Policy (CSP)

### CSRF защита
- Используй токены для форм
- Валидируй запросы на сервере
- Используй заголовки для API запросов

### Другие меры
- Не храни секреты в клиентском коде
- Валидируй данные на клиенте и сервере
- Ограничивай частоту запросов (rate limiting)

## 🌍 Локализация

### Язык и регион
- **Язык:** Русский (ru)
- **Регион:** Россия (RU)
- **Валюты:** Рубли (₽, руб.)
- **Формат даты:** ДД.ММ.ГГГГ
- **Формат чисел:** пробел как разделитель тысяч (1 250 000)
- **Телефон:** +7 (XXX) XXX-XX-XX

### Тексты
- Используй корректную русскую типографику (тире, кавычки)
- Проверяй орфографию и пунктуацию
- Используй профессиональную терминологию строительной отрасли

## 📋 Специфика лендинга

### Конверсия
- **Приоритет:** получение заявок и контактов
- Все CTA должны быть заметными и понятными
- Минимум шагов до отправки формы
- Социальные доказательства (отзывы, кейсы) на видном месте

### Формы на лендинге
- **Простая форма:** имя + телефон (для быстрых заявок)
- **Расширенная форма:** дополнительно площадь, тип дома, формат отделки
- **Форма калькулятора:** автоматически заполняется после расчёта
- Показывай прогресс заполнения формы
- Предотвращай потерю данных при ошибке

### Калькулятор
- Простой и понятный интерфейс
- Результат показывай сразу при изменении параметров
- Показывай диапазон цен "от X до Y"
- После расчёта предлагай оставить заявку для точной стоимости
- Сохраняй введённые данные в localStorage для восстановления

### Якорная навигация
- Плавная прокрутка при клике на якорные ссылки
- Активное состояние меню при прокрутке секций
- Липкое (sticky) меню на десктопе
- Мобильное меню-бургер с плавной анимацией

## 🎯 Улучшение UX

### Микро-взаимодействия
- Плавные переходы при hover на кнопки
- Обратная связь при клике (ripple effect, изменение состояния)
- Анимация появления элементов при скролле
- Индикаторы загрузки для всех асинхронных операций

### Сообщения пользователю
- Успешная отправка формы: "Спасибо! Мы свяжемся с вами в течение 15 минут"
- Ошибка: "Произошла ошибка. Пожалуйста, попробуйте ещё раз или позвоните нам"
- Валидация: конкретные сообщения ("Укажите корректный номер телефона")
- Информационные подсказки для сложных полей

### Производительность UX
- Skeleton screens вместо спиннеров для контента
- Progressive loading для изображений
- Приоритет загрузки: сначала контент выше fold
- Preload для критических ресурсов

---

**Примечание:** При работе над проектом всегда помни о цели — получить целевые заявки. Каждый элемент должен работать на конверсию и доверие.

